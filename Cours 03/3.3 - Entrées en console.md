# La fonction `input()` en Python

> [Documentation officielle](https://docs.python.org/fr/3.14/library/functions.html#input)

## Objectifs

- Comprendre comment lire une saisie au clavier.
- Savoir que `input()` retourne toujours une chaîne de caractères (`str`).
- Apprendre à convertir la saisie en `int` ou `float` quand nécessaire.
- Connaître les erreurs courantes liées à la conversion.

## Présentation de `input()`

La fonction `input()` est une **fonction native** de Python : elle est disponible sans importation.

> **Syntaxe** : `valeur_saisie = input("Message de saisie : ")`

Lors de l'utilisation de cette fonction, le programme marque une pause après l'affichage du message de saisie et reprend quand l'utilisateur valide la saisie avec la touche `ENTRÉE`.

```python
nom = input("Quel est ton nom ? ")
# le programme attend la saisie de l'utilsateur et stocke 
```

La chaîne affichée entre guillemets est le **message de saisie** présenté à l'utilisateur.

## `input()` retourne toujours un `str`

**Peu importe ce que l'utilisateur tape, le résultat est toujours une chaîne de caractères**.

```python
age = input("Quel est ton âge ? ")
print(type(age))  # <class 'str'> — même si l'utilisateur a tapé "25" !
```

Si tu as besoin d'un nombre, tu dois convertir toi-même la chaîne :

```python
age = int(input("Quel est ton âge ? "))       # conversion en entier
prix = float(input("Quel est le prix ? "))    # conversion en nombre décimal
```

## Lire différents types de données

| Code Python | Description |
| --- | --- |
| `nom = input("...")` | Lit une ligne complète (texte) |
| `age = int(input("..."))` | Lit un entier |
| `prix = float(input("..."))` | Lit un nombre à virgule |

## Remarques importantes

1. `input()` lit **toujours une ligne complète**, y compris les espaces.
2. Le message passé à `input("...")` s'affiche automatiquement avant la saisie.
3. Si l'utilisateur tape un texte qui ne peut pas être converti (`int("abc")` par exemple), Python lève un `ValueError`.
4. Pas besoin de `.close()` : Python gère la fermeture de l'entrée automatiquement.

## Exemple complet

```python
nom = input("Quel est ton nom ? ")
age = int(input("Quel est ton âge ? "))
print(f"Bonjour {nom}, tu as {age} ans.")
```

## Exercice

1. Que fait ce programme si l'utilisateur tape `Alice` puis `20` ?
2. Écris un programme qui demande un nombre, ajoute 5 à ce nombre, puis affiche le résultat.
3. Pourquoi ne faut-il pas oublier la conversion `int()` si on veut additionner deux nombres lus avec `input()` ?

## À retenir

- `input()` lit du texte et retourne une `str`.
- Pour obtenir un entier, utiliser `int(input(...))`.
- Pour obtenir un nombre décimal, utiliser `float(input(...))`.
- Les conversions peuvent produire une erreur si la saisie n'est pas au bon format.

## 🎥 Vidéo explicative : La fonction `input()`

[![Regarder](https://img.youtube.com/vi/GqDUMIcsqdQ/maxresdefault.jpg)](https://youtu.be/GqDUMIcsqdQ)

*Cette vidéo explique comment utiliser `input()` pour lire une saisie au clavier en Python.*
