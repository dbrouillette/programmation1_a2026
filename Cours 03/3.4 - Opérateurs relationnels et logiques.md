# Opérateurs relationnels (de comparaison) et opérateurs logiques en Python

## Objectifs

- Comprendre comment comparer des valeurs en Python.
- Connaître les opérateurs relationnels et logiques de base.
- Savoir combiner des comparaisons avec `and`, `or` et `not`.
- Utiliser les parenthèses pour clarifier les expressions.

## 1. Opérateurs relationnels (de comparaison)

Les opérateurs de comparaison permettent de **comparer deux valeurs**. Ils retournent un booléen (`True` ou `False` — **notez la majuscule**).

|Opérateur|Signification|Exemple (`a = 5, b = 3`)|Résultat|
|:---:|---|:---:|:---:|
|`>`|Supérieur à|`a > b`|`True`|
|`<`|Inférieur à|`a < b`|`False`|
|`>=`|Supérieur ou égal à|`a >= b`|`True`|
|`<=`|Inférieur ou égal à|`a <= b`|`False`|
|`==`|Égal à|`a == b`|`False`|
|`!=`|Différent de|`a != b`|`True`|

## ✅ `==` fonctionne sur les chaînes de caractères

**`==` compare le contenu des valeurs**. Cela vaut aussi pour les chaînes.

```python
nom1 = "Alice"
nom2 = "Alice"
print(nom1 == nom2)  # True
```

En revanche, `=` sert à affecter une valeur à une variable :

```python
nom = "Alice"  # affectation
```

### 🎥 Vidéo explicative : Opérateurs de comparaison

[![Regarder](https://img.youtube.com/vi/QwU-w8gFoJc/maxresdefault.jpg)](https://youtu.be/QwU-w8gFoJc)

*Cette vidéo explique les opérateurs de comparaison en Python.*

## 2. Opérateurs logiques

Les opérateurs logiques permettent de **combiner des expressions booléennes**.

⚠️ Python utilise des mots-clés en anglais : `or`, `and`, `not`.

|Opérateur Python|Nom|Description|
|:---:|---|---|
|`or`|OU logique|`True` si au moins une des deux expressions est vraie|
|`and`|ET logique|`True` si les deux expressions sont vraies|
|`not`|Négation|Inverse la valeur booléenne|

### Exemples

```python
age = 20
est_etudiant = True
print(age >= 18 and est_etudiant)  # True
print(age < 18 or est_etudiant)     # True
print(not est_etudiant)             # False
```

## 3. Tables de vérité

### 3.1 OU logique (`or`)

|Opérande gauche|OU|Opérande droite|Résultat|
|---:|:---:|:---|:---:|
|`True`|`or`|`True`|`True`|
|`True`|`or`|`False`|`True`|
|`False`|`or`|`True`|`True`|
|`False`|`or`|`False`|`False`|

### 3.2 ET logique (`and`)

|Opérande gauche|ET|Opérande droite|Résultat|
|---:|:---:|:---|:---:|
|`True`|`and`|`True`|`True`|
|`True`|`and`|`False`|`False`|
|`False`|`and`|`True`|`False`|
|`False`|`and`|`False`|`False`|

### 3.3 Négation logique (`not`)

|Négation|Opérande|Résultat|
|---:|:---:|:---:|
|`not`|`True`|`False`|
|`not`|`False`|`True`|

## 4. Priorités des opérateurs

Si une expression contient plusieurs opérateurs, Python applique d'abord `not`, puis les comparaisons, puis `and`, puis `or`.

```python
a = 5
b = 3
c = 10
print(a > b and b < c or a == 5)
```

Pour mieux lire ce type d'expression, il est recommandé d'utiliser des parenthèses :

```python
print((a > b and b < c) or a == 5)
```

## 5. Exemple complet

```python
score = 15
est_adulte = True
if score >= 10 and est_adulte:
    print("Accès autorisé")
elif score >= 10 and not est_adulte:
    print("Accès autorisé avec accompagnement")
else:
    print("Accès refusé")
```

### 🎥 Vidéo explicative : Opérateurs logiques

[![Regarder](https://img.youtube.com/vi/zpVoW3ygas8/maxresdefault.jpg)](https://youtu.be/zpVoW3ygas8)

*Cette vidéo explique les opérateurs logiques `and`, `or` et `not` en Python.*

## Exercice

1. Que vont afficher ces expressions ?

    ```python
    print(5 > 3 and 2 == 2)
    print(5 > 3 or 2 == 3)
    print(not (5 == 5))
    ```

2. Écris une condition qui vérifie si un nombre `n` est compris entre 1 et 10 inclus.

3. Pourquoi vaut-il mieux écrire `if (a > b) and (b > c):` plutôt que `if a > b and b > c:` quand l'expression est longue ?

## À retenir

- `>` , `<` , `>=` , `<=` , `==` , `!=` comparent des valeurs et retournent `True` ou `False`.
- `and`, `or` et `not` permettent de combiner des tests.
- Les parenthèses améliorent la lisibilité des expressions complexes.
- `=` est une affectation, `==` est une comparaison.
